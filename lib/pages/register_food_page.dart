import 'package:flutter/material.dart';

class RegisterFoodPage extends StatelessWidget {
  const RegisterFoodPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: null,
      body: Center(
        child: Text('Register Food Page'),
      ),
    );
  }
}
